/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "static.nike.com",
      "static.zara.net",
      "lp2.hm.com",
      "in.louisvuitton.com",
    ],
  },
};

export default nextConfig;
